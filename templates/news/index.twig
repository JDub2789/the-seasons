{% for entry in craft.entries.section('news').all() %}
  
  <h2><a href="{{ entry.url() }}">{{ entry.title }}</a></h2>
  <p class="margin-bottom-50"><a href="{{ entry.url() }}">Read more...</a></p>

{% endfor %}